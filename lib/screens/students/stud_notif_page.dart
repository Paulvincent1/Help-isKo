import 'package:flutter/material.dart';

class StudNotifPage extends StatelessWidget {
  const StudNotifPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}